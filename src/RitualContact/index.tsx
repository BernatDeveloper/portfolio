import { SectionTitle } from "../utils/SectionTitle/SectionTitle"
import { RitualContact } from "./RitualContact"

export const RitualContactComp = () => {
  return (
    <>
        <SectionTitle ghost="Contact" eyebrow="Get in touch" title="Contact" align="center"/>
        <RitualContact />
    </>
  )
}
